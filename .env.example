# Claude API Configuration
ANTHROPIC_API_KEY=your_api_key_here

# OpenAI / Codex CLI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Gemini/Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your_gcp_project_id_here

# Optional: Specify Claude model
# ANTHROPIC_MODEL=claude-3-5-sonnet

# Development Environment
COMPOSE_PROJECT_NAME=claude-dev

# Docker Configuration
# Set DOCKER_GID to match your host's docker group ID (check with: getent group docker | cut -d: -f3)
# DOCKER_GID=999

# Optional: Multi-instance port overrides
# PORT_OFFSET=0               # Adds to base ports (3001/4001/5001/5433/6380/9201/9301)
# HOST_PORT_FE=3001           # Overrides frontend host port
# HOST_PORT_BE=4001           # Overrides backend host port
# HOST_PORT_MCP=5001          # Overrides MCP host port
# HOST_PORT_PG=5433           # Overrides Postgres host port
# HOST_PORT_REDIS=6380        # Overrides Redis host port
# HOST_PORT_ES_HTTP=9201      # Overrides Elasticsearch HTTP port
# HOST_PORT_ES_TRANSPORT=9301 # Overrides Elasticsearch transport port
